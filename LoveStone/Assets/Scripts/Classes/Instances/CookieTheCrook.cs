using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CookieTheCrook : CharacterCard
{
    // Start is called before the first frame update
    void Start()
    {
        base.AssignInfo();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
